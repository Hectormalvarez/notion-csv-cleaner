[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "notion-csv-cleaner"
version = "0.1.0"
description = "Validate, sanitize, and format CSV exports for direct Notion database imports."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
notion-clean = "notion_csv_cleaner.cli:main"
